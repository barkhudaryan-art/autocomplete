# Tech Stack

- **UI:** React 19
- **Build:** rspack, Babel
- **Styles:** SCSS + CSS Modules
- **Tests:** Jest + React Testing Library
- **Lint:** ESLint flat config (`eslint.config.mjs`)

When a dependency changes (React, Webpack, Babel, ESLint, Jest, etc.), update all references in the same change.
