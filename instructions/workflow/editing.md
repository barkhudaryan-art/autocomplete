# Edit Workflow

- Re-read any existing file immediately before editing — context may be stale.
- Keep edits minimal and scoped; do not revert unrelated changes.
- Implementation: follow `code/react.md`, `code/formatting.md`, ESLint config.
